cd "C:\Users\James Ian\Projects\OnlineJobPH-Assignment"

@"
services:
  mysql:
    image: mysql:8.0
    container_name: food_search_mysql
    restart: always
    environment:
      MYSQL_ROOT_PASSWORD: rootpassword
      MYSQL_DATABASE: food_search_db
      MYSQL_USER: app_user
      MYSQL_PASSWORD: app_password
    ports:
      - "3306:3306"
    volumes:
      - mysql_data:/var/lib/mysql
    healthcheck:
      test: ["CMD", "mysqladmin", "ping", "-h", "localhost", "-u", "app_user", "-papp_password"]
      timeout: 20s
      retries: 10

volumes:
  mysql_data:
"@ | Out-File -FilePath "docker-compose.yml" -Encoding utf8

docker compose up -d